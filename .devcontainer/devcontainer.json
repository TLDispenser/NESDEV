{
	"image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
	"features": {
		"ghcr.io/bascodes/devcontainer-features/desktop:0": {},
		"ghcr.io/bascodes/devcontainer-features/desktop-init:0": {}
	},
	"forwardPorts": [6080],
	"postCreateCommand": "sudo apt-get update && sudo apt-get install -y cc65 fceux libgtk2.0-0 libqt5widgets5 wget unzip && wget -q https://github.com/SourMesen/Mesen2/releases/download/2.1.1/Mesen_2.1.1_Linux_x64.zip -O /tmp/mesen2.zip && sudo mkdir -p /opt/mesen2 && sudo unzip -o /tmp/mesen2.zip -d /opt/mesen2 && sudo ln -sf /opt/mesen2/Mesen /usr/local/bin/mesen2 && sudo chmod +x /opt/mesen2/Mesen"
}